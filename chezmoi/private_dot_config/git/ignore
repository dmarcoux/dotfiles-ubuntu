# From nohup
nohup.out

# Swap file
*.swp

# Bash
.bash_history

# Local nix-shell whenever a project doesn't have a development environment
shell.local.nix

# nix-shell setup for Elixir
.nix-mix.local
.nix-hex.local

# Mise configuration file for local configuration, whenever a project doesn't use Mise and upstreaming this configuration isn't an option
mise.local.toml

# Ruby, this is almost always included in .gitignore, but just in case I ignored it too...
.bundle

# JetBrains IDEs
.idea

# Lock files from LibreOffice
.~lock.*.odt
