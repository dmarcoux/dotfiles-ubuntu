# Setup network drives

# Install dependencies
sudo apt-get install --assume-yes cifs-utils

# Create the mount point directory
sudo mkdir --parents /mnt/L_JobRad

# Search for "How to Mount Windows Network Drives on Linux" in the work's wiki for the next steps.
